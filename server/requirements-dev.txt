# Development and test dependencies — not needed in production
-r requirements.txt

# httpx moved to requirements.txt: it is now the inference transport, not just a
# test client. Tests additionally use httpx.MockTransport to exercise the llama-server
# client without a socket or a model.
pytest==8.3.3
pytest-asyncio==0.24.0
